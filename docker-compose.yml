services:
  postgres:
    image: postgres:16
    container_name: postgres
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: deal
    ports:
      - "5432:5432"
    volumes:
    - ./data:/var/lib/postgresql/data
    restart: always